exp_name:
seed: 1
torch_deterministic: False
cuda: True
track: False
capture_video: True
env_id: vssStrat-v0
num_envs: 10
total_timesteps: 1e7

wandb:
  project_name: 'cleanRL'
  entity:

hydra:
  job:
    chdir: True

agents:
  ddpg:
    - b_0

defaults:
  - agent@ddpg: DDPG
  - _self_
